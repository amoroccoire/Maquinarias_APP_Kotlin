package com.example.maquinariasapp.ui.linProduccion

import android.content.Context
import androidx.lifecycle.ViewModel
import androidx.lifecycle.ViewModelProvider

/*class LineaProduccionViewModelFactory(private val context: Context): ViewModelProvider.Factory {
    override fun <T : ViewModel> create(modelClass: Class<T>): T {
        if (modelClass.isAssignableFrom(LineaProduccionViewModel::class.java)) {
            @Suppress("UNCHECKED_CAST")
            return LineaProduccionViewModel(context) as T
        }
        throw IllegalArgumentException("Unknown ViewModel class")
    }
}*/
class LineaProduccionViewModelFactory(){
}